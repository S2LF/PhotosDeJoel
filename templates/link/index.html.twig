{% extends 'base.html.twig' %}

{% block title %}Les Photos de Joël | Liens{% endblock %}

{% block body %}
<section class="row text-center">
    {% if links|length == 0 %}
    <p>
        <h3 class="text-center">
            Aucun lien
        </h3>
    </p>
    {% else %}
        <section class="m-auto">
            {% for links in links %}
                <article class="m-3">
                    <div class="p-2 d-flex">
                        <div class="d-flex align-items-center">
                            <h3>{{links.titre}}</h3>&nbsp;-&nbsp;
                            <a href="{{ links.lien | raw}}" target="_blank">
                                Voir le site
                            </a>                        
                        </div>
                    </div>
                </article>
            {% endfor %}
        </section>    
    {% endif %}
</section>

{% endblock %}